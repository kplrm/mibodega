// JS for productos template

/*    if ($("#checkout_form").validate().form() && {{cart_obj.crt_total_price}} != 0 && usr_geolocation != "") {
        // submit with AJAX
        $.ajax({
            headers: {'X-CSRFToken': '{{ csrf_token }}'},
            url: "/submit_checkout",
            type: "POST",
            dataType: "json",
            data:   {
                        'csrfmiddlewaretoken': csrftoken,
                        'cart_obj_id': cart_obj_id.toString(),
                        'usr_first': usr_first,
                        'usr_last': usr_last,
                        'usr_street': usr_street,
                        'usr_geolocation': usr_geolocation,
                        'usr_email': usr_email,
                        'usr_phone': usr_phone,
                        'usr_comments': usr_comments,
                        // spare line to send confirmation if user wants to update his preferences
                    }
        });
    } else {
        alert("Seleccione en el mapa su ubicación");
    }*/
};

$(document).ready(function(){

});

