sudo apt-get install python3-venv
python3 -m venv eshop
sudo apt-get install qgis
pip install six
pip install cachetools
pip install requests
pip install wheel
pip install ipregistry
pip install django
pip install django-widget-tweaks
pip install psycopg2-binary #better than psycopg2 since it requires one less depencedy
pip install Pillow
django-admin.py startproject shops .
python manage.py startapp nearbyshops
python manage.py createsuperuser
python manage.py makemigrations nearbyshops --empty
